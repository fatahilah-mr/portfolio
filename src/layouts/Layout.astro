---
import '../styles/global.css';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import FloatingLang from '../components/FloatingLang.astro';

export interface Props {
  title: string;
  description?: string;
}

const { 
  title, 
  description = "Portofolio Fatahilah Miftahul Rahman - Juara 1 LKS IT Network System Administration & AI Prompt Engineering Specialist. Membangun infrastruktur server stabil dan otomatisasi alur kerja AI."
} = Astro.props;
---

<!DOCTYPE html>
<html lang="id" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <meta name="author" content="Fatahilah Miftahul Rahman" />
  <link rel="icon" type="image/svg+xml" href="/assets/favicon/favicon.svg" />
  <title>{title} | Fatahilah Miftahul Rahman</title>

  <!-- Canonical URL -->
  <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website" />
  <meta property="og:url" content={Astro.url} />
  <meta property="og:title" content={`${title} | Fatahilah Miftahul Rahman`} />
  <meta property="og:description" content={description} />

  <!-- Twitter -->
  <meta property="twitter:card" content="summary_large_image" />
  <meta property="twitter:url" content={Astro.url} />
  <meta property="twitter:title" content={`${title} | Fatahilah Miftahul Rahman`} />
  <meta property="twitter:description" content={description} />

  <!-- Schema.org JSON-LD (AI Entity & Knowledge Graph Optimization) -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "Person",
    "name": "Fatahilah Miftahul Rahman",
    "url": "https://fatahmr.my.id",
    "image": "https://cdn.fatahmr.my.id/portfolio/assets/profile.webp",
    "jobTitle": "IT Network System Administration & Web Development Specialist",
    "alumniOf": {
      "@type": "EducationalOrganization",
      "name": "SMK Patriot Pituruh"
    },
    "award": [
      "Juara 1 Lomba Kompetensi Siswa (LKS) IT Network System Administration Kabupaten Purworejo 2026"
    ],
    "knowsAbout": [
      "IT Network System Administration",
      "Cisco Networking & Switching",
      "MikroTik RouterOS & Hotspot Gateway",
      "Linux Server Administration (Debian 12)",
      "Windows Server 2022 & ADDS",
      "AI Prompt Engineering & Automation",
      "Web Development (Astro, HTML, Vanilla CSS)"
    ],
    "sameAs": [
      "https://github.com/fatahilah-mr",
      "https://www.linkedin.com/in/fatahilah-mr"
    ]
  })} />

  <!-- Performance Optimization: Preconnect & Load Fonts in parallel (DTK-PERF-001) -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="preload" href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300..800;1,9..40,300..800&family=DM+Serif+Display:ital@0;1&family=JetBrains+Mono:wght@400;500;600;700&display=swap" as="style" />
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300..800;1,9..40,300..800&family=DM+Serif+Display:ital@0;1&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet" />

  <!-- Anti-FOUC Theme & Language Initialization Script -->
  <script is:inline>
    (function() {
      try {
        const theme = localStorage.getItem('theme') || 'light';
        document.documentElement.setAttribute('data-theme', theme);

        const lang = localStorage.getItem('lang') || 'id';
        document.documentElement.setAttribute('lang', lang);
      } catch (e) {
        console.warn('LocalStorage error during theme init:', e);
      }
    })();
  </script>
</head>
<body>
  <!-- Accessible Skip Link (DTK-A11Y-001) -->
  <a href="#main-content" class="skip-to-content sr-only focus:not-sr-only">
    Skip to main content / Langsung ke konten utama
  </a>

  <!-- ARIA Live Region for Dynamic Announcements (DTK-A11Y-002) -->
  <div id="a11y-announcer" class="sr-only" aria-live="polite" aria-atomic="true"></div>

  <!-- Navigation -->
  <Navbar />

  <!-- Main Content Slot with Programmatic Focus Target -->
  <main id="main-content" tabindex="-1" class="main-wrapper">
    <slot />
  </main>

  <!-- Floating Language Switcher -->
  <FloatingLang />

  <!-- Footer -->
  <Footer />

  <style>
    .skip-to-content:focus {
      position: fixed;
      top: 16px;
      left: 16px;
      z-index: 9999;
      background-color: var(--color-primary);
      color: #FFFFFF;
      padding: 12px 24px;
      border-radius: 8px;
      font-weight: 700;
      box-shadow: var(--shadow-lg);
      outline: 3px solid #FFFFFF !important;
    }

    .main-wrapper {
      flex: 1;
      display: flex;
      flex-direction: column;
      outline: none;
    }
  </style>
</body>
</html>
